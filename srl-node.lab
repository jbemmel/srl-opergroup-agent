name: opergroup-lab

topology:
  kinds:
    srl:
      image: srl/opergroup-agent:latest
      binds:
      - ./src:/opt/demo-agents

  nodes:
    spine1:
      kind: srl
      startup-config: spine1.cfg
      # Can make SDK Unix socket available on host, requires user with uid 1002
      # binds:
      # - /tmp/spine1:/opt/srlinux/var/run
